# HausTap Root MVC App (Public)

Serves the root MVC PHP app.

## Run Locally

- `php -S localhost:8001 -t public`
- Open `http://localhost:8001/`

## Structure

- `core/` — Router and View utilities
- `app/` — Controllers and application code

This document does not modify UI/UX.
