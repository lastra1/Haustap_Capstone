APP_NAME=HausTap
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost:8080

# Database (for Laravel backend)
DB_CONNECTION=sqlite
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=

# Redis (optional)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=0

# Mail / SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=ssl
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_FROM_NAME=HausTap

# Logging
LOG_LEVEL=debug

